# TreeSheets 中文待办清单

这个文档维护当前仓库里更可执行的迭代清单。根目录的 `TODO.txt` 更像上游历史想法池，里面有很多松散点子；这里按模块拆成可以验证、可以继续实现的任务。

状态说明：

- 已完成：已经实现并已有基础验证。
- 未完成：仍需要补实现、补测试、补交互或补平台验证。

## 已完成

### 测试与持续集成

- [x] 新增 `TREESHEETS_BUILD_TESTS` 构建选项，默认启用测试构建。
- [x] 新增聚合目标 `TreeSheetsTests`，一次构建文本、网格、序列化、文档四组测试。
- [x] 在测试构建中启用 `TREESHEETS_TESTING`，避免测试二进制重复注册 wxWidgets 应用入口。
- [x] 在测试构建中保留事件表编译，避免菜单、命令、窗口事件符号缺失。
- [x] 为主程序和测试目标补充头文件依赖，解决只改头文件时增量构建不重新编译的问题。
- [x] GitHub Actions 在 Linux、Windows、macOS 上构建主程序。
- [x] GitHub Actions 在 Linux、Windows、macOS 上构建并运行测试。

### 文本核心

- [x] 补充文本对象的基础单元测试。
- [x] 验证普通字符和多字节字符的光标移动。
- [x] 验证组合字符、零宽连接符表情、区域指示符旗帜等字素簇删除行为。
- [x] 验证控制字符和特殊字符的显示转义。
- [x] 验证硬换行、软换行、自动换行的基础布局行为。
- [x] 验证退格删除不会破坏完整字素簇。

### 网格核心

- [x] 验证合并单元格修复逻辑。
- [x] 验证选择范围归一化逻辑。
- [x] 验证复制选择区域时保留合并结构。
- [x] 验证复制选择区域时保留边框样式。
- [x] 验证插入列、删除列后的网格尺寸和内容移动。
- [x] 验证选择区域边框设置。
- [x] 验证转置操作保留内容和边框。
- [x] 验证按选中列排序的基础行为。

### 保存、读取与序列化

- [x] 拆出 `ReadDB`，让文件读取可以脱离窗口直接测试。
- [x] 拆出 `LoadDBIntoDocument`，让读取结果可以加载到指定文档对象。
- [x] 新增 `LoadedDB` 结构，集中保存读取出的根单元格、选中单元格、尺寸、缩放、统计信息和标签。
- [x] 读取数据库文件时检查魔数、版本、尺寸、缩放和块头短读。
- [x] 损坏文件、截断文件会返回错误，不再继续解析未完整读取的数据。
- [x] `SaveDB` 在失败路径会明确把 `success` 置为 `false`。
- [x] 验证单元格二进制读写往返。
- [x] 验证网格二进制读写往返。
- [x] 验证没有选中标记时读取逻辑不会崩溃。
- [x] 验证 `.cts` 保存后可以重新读取。
- [x] 验证标签可以随文件保存和读取。
- [x] 验证 XML 导入的基础设置和样式。
- [x] 验证 CSV 导入支持引号、跨行字段、尾部空字段、头部空字段。
- [x] 修复 CSV 导入尾部空字段丢失的问题。
- [x] 修复 CSV 导入行数不足时可能越界写入的问题。
- [x] CSV 导入改用分隔符长度推进，支持多字符分隔符的基础场景。

### 文档编辑行为

- [x] 支持无窗口测试环境下创建和操作 `Document`。
- [x] `TSFrame` 缺失时，状态栏更新和文件名更新不会访问空指针。
- [x] `Canvas` 缺失时，撤销、重做、光标设置不会访问空指针。
- [x] 验证文本编辑撤销、重做。
- [x] 验证网格结构变化撤销、重做。
- [x] 验证同一代编辑可以合并成一次撤销记录。
- [x] 验证粘贴制表符分隔文本会生成网格。
- [x] 验证粘贴单段文本会写入当前单元格。
- [x] 验证文本编辑态粘贴会规范化换行。
- [x] 验证 HTML 表格粘贴支持基础跨行、跨列和样式。
- [x] 验证 CSV 导出转义。
- [x] 验证 XML 导出基础结构。
- [x] 验证 HTML 导出基础样式。
- [x] 验证层级化和扁平化可以往返。

### 文件变更监控

- [x] 将外部文件变更重载逻辑拆成可复用辅助函数。
- [x] 文件系统事件触发时使用统一重载逻辑。
- [x] `SaveCheck` 增加轮询兜底，减少文件系统事件漏报后无法发现外部修改的问题。
- [x] 无窗口环境下 `FileUsed` 不再依赖文件监控器对象。

## 高优先级待办

### 测试基础继续收敛

- [x] 抽出共享测试辅助头文件，减少四组测试里重复的启动、文档创建、网格断言代码。
- [x] 给测试辅助函数补中文命名或中文注释，让新增测试更容易写。
- [x] 把常用 `.cts`、`.html`、`.csv` 样本放进固定测试夹具目录。
- [x] 为测试夹具补充最小、损坏、旧版本、复杂样式四类样本；其中二进制 `.cts` 的最小、旧版本、复杂样式样本由测试代码生成，损坏样本落盘保存。
- [x] 在 README 或开发文档里写清楚本地运行测试的命令。
- [x] 评估是否把 `main.cpp` 的测试引入方式拆成更干净的库目标。

### 文件格式与兼容性

- [x] 补旧版本 `.cts` 文件读取测试，覆盖版本升级分支。
- [x] 补包含图片的 `.cts` 保存和读取往返测试。
- [x] 补图片损坏或图片缺失时的读取降级测试。
- [x] 补大文件读取的基础性能和内存回归测试。
- [x] 补保存失败路径测试，例如目标不可写、临时文件写入失败、重命名失败。
- [x] 补保存过程中统计信息正确性的测试：单元格数量、文本字节数、图片数量。
- [x] 补读取非法尺寸、非法缩放、非法块长度时的防御性测试。
- [x] 补读取空文件、随机字节文件、只含文件头文件的测试。

### 自动保存与恢复

- [x] 梳理自动保存文件的位置、命名、清理策略。
- [x] 补自动保存生成测试。
- [x] 补异常退出后恢复自动保存文件的测试。
- [x] 补正常保存后清理自动保存文件的测试。
- [x] 补多个打开文档同时自动保存时的隔离测试。
- [ ] 在界面上明确恢复提示，避免静默覆盖用户当前文件。

### 外部文件变更

- [x] 为外部修改轮询逻辑补单元测试或轻量集成测试。
- [ ] 验证 Windows 文件监控事件和轮询兜底不会重复弹窗。
- [ ] 验证 macOS 文件监控事件和轮询兜底不会漏掉快速连续保存。
- [ ] 验证 Linux 文件监控事件在替换式保存、原地保存两种场景下都能触发。
- [ ] 补用户拒绝重载后的后续行为测试。
- [ ] 补文件已删除、移动、重命名后的提示逻辑。

### 编辑和选择

- [x] 补合并单元格和取消合并单元格的文档层测试。
- [x] 补删除行、删除列、删除选区的文档层测试。
- [x] 补退格键在文本开头、单元格开头、选区内的行为测试。
- [x] 补方向键、Home、End、PageUp、PageDown 的选择扩展测试。
- [x] 补按词移动、按词删除的 Unicode 行为测试。
- [x] 补多行文本里 Shift 选择和鼠标拖选的行为测试。
- [x] 补单元格折叠、展开、全部折叠、全部展开的测试。
- [x] 补父子层级包裹、拆分、提升、降级的测试。
- [x] 补拖拽移动单元格和拖拽复制单元格的测试。
- [x] 补复制、剪切、粘贴跨文档场景。

### 导入与导出

- [x] 补制表符分隔、分号分隔、自定义分隔符的 CSV 导入测试。
- [x] 补 CSV 字段内包含分隔符、引号、换行、空白前后的测试。
- [x] 补 HTML 导入嵌套表格测试。
- [x] 补 HTML 导入图片、链接、颜色、字体大小的测试。
- [x] 补 HTML 导出图片和链接。
- [x] 补 XML 导出更多样式字段。
- [x] 补 JSON 导出，便于和脚本、网页工具集成。
- [ ] 评估 JSON 导入是否值得实现。
- [ ] 评估 OPML 导入导出是否保留为长期目标。
- [ ] 评估 RTF 导出是否仍有现实价值。

### 用户界面与平台体验

- [x] 修复或完善资源管理器面板。
- [x] 实现文件搜索。
- [ ] 支持按文件名、内容、标签过滤。
- [x] 支持文件搜索结果点击后打开对应文档。
- [ ] 支持最近文件、收藏文件、工作区文件列表。
- [ ] 补 macOS 输入法组合输入测试。
- [ ] 补高分屏缩放下的字体、网格线、图片显示检查。
- [ ] 补多显示器下窗口位置恢复检查。
- [ ] 补暗色模式下颜色对比检查。
- [ ] 补菜单、工具栏、快捷键可用性检查。
- [ ] 补打印预览和分页显示的基础回归。

### 常用功能

- [ ] 支持更完整的换行编辑体验。
- [ ] 支持纯文本粘贴，不带样式。
- [ ] 支持只复制图片。
- [ ] 支持插入当前日期、当前时间。
- [ ] 支持日期序列、数字序列自动填充。
- [ ] 支持单元格字数统计和选区字数统计。
- [ ] 支持基础拼写检查。
- [ ] 支持模板或新手示例文档。
- [ ] 支持打开外部文本文件并和单元格内容同步。
- [ ] 支持外部图片懒加载，降低大文档打开成本。
- [ ] 支持批量替换。
- [ ] 支持在当前文档内保存搜索结果视图。

### 质量、稳定性与安全

- [ ] 对递归网格操作增加更明确的深度保护。
- [ ] 对所有文件读取长度做统一边界检查。
- [ ] 对图片解码失败路径做统一错误收集。
- [x] 对撤销栈大小增加压力测试。
- [x] 对大规模复制、粘贴、排序增加性能测试。
- [x] 对内存占用增加简单基准记录。
- [x] 评估地址检查、未定义行为检查、静态分析能否加入持续集成。
- [ ] 梳理异常处理和用户提示的中文文案。
- [ ] 梳理临时文件、备份文件、恢复文件的生命周期。

## 中优先级待办

### 视图和导航

- [ ] 支持更清晰的缩放层级提示。
- [ ] 支持快速跳转到父单元格、根单元格、上一次编辑位置。
- [ ] 支持当前单元格路径面包屑。
- [ ] 支持大纲视图和表格视图之间更平滑切换。
- [ ] 支持选区内临时聚焦视图。
- [ ] 支持更清楚的冻结标题或固定上下文。

### 样式和格式

- [ ] 支持样式刷。
- [ ] 支持样式预设。
- [ ] 支持清除格式但保留内容。
- [ ] 支持更细的边框选择。
- [ ] 支持颜色最近使用列表。
- [ ] 支持按标签自动着色。
- [x] 支持图片显示尺寸的无损基础编辑。
- [ ] 支持图片裁剪的基础编辑。

### 数据和自动化

- [ ] 评估公式功能的范围，先从简单引用和聚合开始。
- [ ] 评估查询功能是否作为普通用户功能开放。
- [ ] 评估脚本接口的安全边界。
- [ ] 支持从选区生成简单统计。
- [ ] 支持按列筛选和排序的更完整交互。
- [ ] 支持标签批量编辑。

## 低优先级和长期想法

- [ ] 移动端或触屏优化。
- [ ] 双栏或多栏文件浏览。
- [ ] 插件系统。
- [ ] 文档加密。
- [ ] 实时协作。
- [ ] 云同步。
- [ ] Web 版本。
- [ ] 更完整的绘图或标注工具。
- [ ] OpenGL 或硬件加速视图。
- [ ] 面向演示的只读播放模式。
- [ ] 面向发布的静态网页导出。

## 推荐下一轮实现顺序

1. [x] 抽出测试辅助头文件，降低继续补测试的成本。
2. [x] 补合并、取消合并、删除行列、折叠展开的文档层测试。
3. [x] 补自动保存和恢复相关测试。
4. [x] 补图片保存、读取、损坏降级测试。
5. [x] 整理资源管理器面板，先实现文件搜索。
6. [x] 再处理旧版本 `.cts` 兼容样本。
7. [x] 补大文件压力测试。
